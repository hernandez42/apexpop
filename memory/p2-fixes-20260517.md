# P2 Bug 修复记录 — 2026-05-17

## 修复 1：并发文件锁接入
**文件**: `scripts/self-evolve-loop.py`
**问题**: `load_experience()` 和 `save_experience()` 使用原生 `open()`/`json.load()`/`json.dump()`，无并发保护
**修复**:
- 导入 `safe-io.py` 的 `safe_load_json` 和 `safe_save_json`
- `load_experience()` 改为调用 `safe_load_json(EXPERIENCE_FILE, default=None)`
- `save_experience()` 改为调用 `safe_save_json(EXPERIENCE_FILE, data)`
- 验证：语法检查通过，导入链正常，读写 16 条经验记录正常

## 修复 2：模板补全
**文件**: `scripts/self-propose.py`
**问题**: `task_templates` 只有 4 个领域（代码能力、系统架构、安全防护、数据分析），DOMAINS 有 8 个
**修复**: 补全 4 个缺失领域的任务模板：
- **用户交互**：交互流程设计、交互痛点分析、无障碍交互规范
- **知识管理**：知识检索策略、知识图谱 schema、知识库存储优化
- **进化算法**：自适应变异策略、多目标 Pareto 搜索、种群多样性分析
- **多Agent协作**：任务分配策略、消息传递协议、冲突检测消解
- 验证：语法检查通过，DOMAINS 列表与 task_templates 完全匹配（8/8）

## 修复 3：SOUL.md 去重
**文件**: `SOUL.md`
**问题**: "核心机密" 段落出现 3 次（2026-05-15 固化 × 1 + 2026-05-16 固化 × 1 + Session Context 引用 × 1），其中前两处内容完全一致（各约 30 行）
**修复**: 删除 `## 🔐 核心机密（2026-05-15 固化）` 整个段落（30 行），保留较新的 2026-05-16 版本
**效果**: 文件从约 750 行减少到 617 行（减少约 133 行），"核心机密" 从 3 次降为 2 次（含 Session Context 中的引用）

## 修复 4：self-modify-code 沙箱死代码清理
**文件**: `scripts/self-modify-code.py`
**问题**: `apply_if_safe()` 方法从未被 `evolve()` 调用，且方法本身不修改文件（仅测试语法+日志记录），属于死代码
**修复**: 删除 `apply_if_safe()` 方法（约 20 行）
- `evolve()` 流程保持不变：identify_bottleneck → generate_improvement → 记录改进
- 验证：语法检查通过，`demo()` 可正常运行
